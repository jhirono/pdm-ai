{
  "mcpServers": {
    "pdm-ai": {
      "command": "node",
      "args": ["./src/mcp/index.js"],
      "env": {
        "PDM_CONFIG_PATH": "${HOME}/.pdm/config.json",
        "LLM_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}