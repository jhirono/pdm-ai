# PDM-AI Configuration

# =================================================
# REQUIRED SETTINGS
# =================================================

# OpenAI API Key (REQUIRED)
# Get yours at: https://platform.openai.com/api-keys
LLM_API_KEY=your_openai_api_key_here

# =================================================
# OPTIONAL SETTINGS
# =================================================

# LLM Model Configuration
# Recommended model is gpt-4o for best results
# Note: Reasoning models like o4-mini may produce less consistent results
LLM_MODEL=gpt-4o

# Maximum tokens for API responses
LLM_MAX_TOKENS=4000

# Temperature setting (0.0-1.0)
# Higher values increase randomness, lower values make output more deterministic
LLM_TEMPERATURE=0.7

# Language Setting
# Supported values: 'en' (English), 'ja' (Japanese)
LANGUAGE=en

# =================================================
# After installation:
# 1. Copy this file to .env: cp .env.example .env
# 2. Edit .env and set your API key
# 3. Run pdm commands normally
# =================================================
